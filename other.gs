// ========================================================================= //
//                         依頼要旨がその他の場合の処理                           //
// ========================================================================= //

function Other() {
  
  console.log("依頼要旨：部品手配");
  
  setSheetForm.getRange('V3').setBackground(fillColor);  // 「その他」塗り潰す。
  setSheetForm.getRange('B40').setValue(AV);             // 依頼内容  

}
